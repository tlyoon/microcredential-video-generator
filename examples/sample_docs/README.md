# Sample documents

`Physics_Laboratory_101_Student_Manual_v2_Corrected.docx` is included only as a **sample/reference input** for inspecting the expected document structure and for explicit tests or demonstrations.

The package never discovers or uses this file automatically. Every runtime build command requires an explicit `--source` argument. Production users should point `--source` to the document they actually intend to convert.
